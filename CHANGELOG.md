# Changelog

## v1.0.0
- Initial production-ready release with Docker support, CI tests and basic webhook security.
- `config.py` centralizes environment loading
- Endpoints secured with HMAC and admin token
- Docker image `gigi-webhook:v1.0.0` created
- Unit tests pass in CI
